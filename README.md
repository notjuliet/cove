# Cove

A simple Jellyfin request manager.

## Setup

Requires [Bun](https://bun.sh).

```sh
bun run setup
bun run start
```

Then open the setup URL printed in the server logs.

The server listens on `PORT` and stores data in `DATA_DIR`.
